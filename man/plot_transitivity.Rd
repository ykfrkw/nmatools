% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_transitivity.R
\name{plot_transitivity}
\alias{plot_transitivity}
\title{Plot Transitivity Assessment}
\usage{
plot_transitivity(
  data,
  studlab,
  treat,
  covariate_cols,
  outcome = "transitivity",
  path = "./outputs",
  n_min_pair = 2L,
  trim = TRUE,
  trim_fuzz = 30L
)
}
\arguments{
\item{data}{Arm-level data frame (same as passed to \code{\link[=netmetawrap]{netmetawrap()}}).}

\item{studlab}{Unquoted or quoted column name for study labels.}

\item{treat}{Unquoted or quoted column name for treatment.}

\item{covariate_cols}{Character vector of column names to visualize.
Only numeric columns are plotted; others are skipped with a message.}

\item{outcome}{Name used for the output sub-directory and file names.
Default \code{"transitivity"}.}

\item{path}{Base output directory. Default \code{"./outputs"}.}

\item{n_min_pair}{Minimum number of studies per comparison to include
that comparison in the plot. Default \code{2L}.}

\item{trim}{Trim whitespace from PDFs via magick. Default \code{TRUE}.}

\item{trim_fuzz}{Fuzz parameter for \code{\link[magick:transform]{magick::image_trim()}}. Default \code{30L}.}
}
\value{
Invisibly, a data frame with one row per study × comparison,
containing the covariate values used for plotting.
}
\description{
For each specified continuous covariate, produces a strip-and-box plot of
study-level covariate values grouped by direct treatment comparison.
Helps assess whether the distribution of effect modifiers differs across
the comparisons in the network (transitivity assumption).
}
\details{
Each study is assigned to all pairwise comparisons it directly contributes
to (i.e., all pairs among the treatments in that study). For a 3-arm study
with arms A, B, C, the study is included in A vs B, A vs C, and B vs C.
}
\examples{
\dontrun{
d <- load_w2i()

plot_transitivity(
  data           = d,
  studlab        = id,
  treat          = t,
  covariate_cols = c("rob", "indirectness"),
  outcome        = "remission_lt",
  path           = "./outputs"
)
}
}
