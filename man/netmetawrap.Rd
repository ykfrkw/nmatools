% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netmetawrap.R
\name{netmetawrap}
\alias{netmetawrap}
\title{One-stop Network Meta-Analysis Wrapper}
\usage{
netmetawrap(
  data,
  studlab,
  treat,
  outcome,
  n,
  event = NULL,
  mean_col = NULL,
  sd_col = NULL,
  sm,
  reference.group = NULL,
  small.values = "undesirable",
  path = "./outputs",
  netmeta_args = list(),
  forest_args = list(),
  netpairwise_args = list(),
  netsplit_args = list(),
  a4_rows_per_page = 45L,
  funnel_min_studies = 10L,
  rare_events = c("auto", "always", "never"),
  trim = TRUE,
  trim_fuzz = 30L,
  .subnet_label = NULL
)
}
\arguments{
\item{data}{Data frame in arm-level format (one row per study arm).}

\item{studlab}{Quoted column name for study labels, e.g. \code{"id"}
(an unquoted name also works in a direct call, but not via
\code{\link[=run_nma_batch]{run_nma_batch()}}).}

\item{treat}{Quoted column name for treatment.}

\item{outcome}{Outcome name used for the output sub-directory and file names.}

\item{n}{Quoted column name for sample size.}

\item{event}{Quoted column name for event count (binary only).}

\item{mean_col}{Quoted column name for arm mean (continuous only).}

\item{sd_col}{Quoted column name for arm SD (continuous only).}

\item{sm}{Effect measure: \code{"OR"}, \code{"RR"}, \code{"SMD"}, or \code{"MD"}.}

\item{reference.group}{Reference treatment (string). \code{NULL} = auto (largest n).}

\item{small.values}{\code{"undesirable"} (default) or \code{"desirable"}.}

\item{path}{Base output directory. Defaults to \code{"./outputs"}.
The function creates \verb{\{path\}/\{outcome\}/} automatically.}

\item{netmeta_args}{Named list of extra arguments forwarded to
\code{netmeta::netmeta()} or \code{netmeta::netmetabin()}, overriding defaults.}

\item{forest_args}{Named list of extra arguments forwarded to all
\code{forest()} calls, overriding defaults.}

\item{netpairwise_args}{Named list of extra arguments forwarded to
\code{netmeta::netpairwise()}.}

\item{netsplit_args}{Named list of extra arguments forwarded to
\code{netmeta::netsplit()}.}

\item{a4_rows_per_page}{Estimated max rows per A4 page for large plots.
Default: \code{45}.}

\item{funnel_min_studies}{Minimum number of studies (k) per pairwise
comparison required to draw a contour-enhanced funnel plot.
Default: \code{10L}.}

\item{rare_events}{One of \code{"auto"} (default), \code{"always"}, \code{"never"}.
Controls the rare-event NMA workflow for binary outcomes:
\itemize{
\item \code{"auto"}: run diagnostics; if \code{rare_flow = TRUE} (low event rate
and/or many zero-arm studies; criteria mirror \code{pmatools}), switch the
primary \code{netmetabin} call to common-effect Mantel-Haenszel without
continuity correction (Efthimiou et al. 2019, Stat Med 38:2992-3012)
and additionally fit a 4-method sensitivity panel
(MH / NCH / IV-FE-CC / IV-RE-CC).
\item \code{"always"}: skip the auto-detection and force the rare-event workflow.
\item \code{"never"}: skip diagnostics and the sensitivity panel entirely;
keep the existing IV default.
For continuous outcomes the argument is ignored.
}}

\item{trim}{Trim whitespace from output PDFs via \code{magick}. Default: \code{TRUE}.}

\item{trim_fuzz}{Fuzz parameter for \code{magick::image_trim()}. Default: \code{30L}.}

\item{.subnet_label}{Internal -- subnetwork label appended to file names.}
}
\value{
Invisibly, the fitted \code{netmeta}/\code{netmetabin} object, or \code{NULL} when
subnetworks are detected.
}
\description{
Runs a complete NMA pipeline and saves all outputs. Column names are given
as quoted strings, e.g. \code{studlab = "id"}. Non-column arguments (\code{sm},
\code{reference.group}, etc.) are always strings.
}
\details{
Note: in a direct \code{netmetawrap()} call the column arguments also accept
unquoted names (like \code{meta::pairwise()}), but \code{\link[=run_nma_batch]{run_nma_batch()}} requires
quoted strings, so quoting everywhere keeps both call styles working.
}
\examples{
\dontrun{
# Quoted column names (recommended: the same style works in run_nma_batch())
netmetawrap(
  data            = w2i_trials,
  studlab         = "id",
  treat           = "t",
  outcome         = "remission_lt",
  n               = "n",
  event           = "r",
  sm              = "OR",
  reference.group = "Pharmacotherapy",
  small.values    = "undesirable"
)

# Continuous outcome
netmetawrap(
  data     = my_data,
  studlab  = "study",
  treat    = "treatment",
  outcome  = "sleep_efficiency",
  n        = "n",
  mean_col = "se_mean",
  sd_col   = "se_sd",
  sm       = "SMD"
)

# Unquoted names are also accepted in a direct call (not via run_nma_batch())
netmetawrap(
  data    = w2i_trials,
  studlab = id,
  treat   = t,
  outcome = "remission_lt",
  n       = n,
  event   = r,
  sm      = "OR"
)
}
}
\seealso{
\code{\link[=run_nma_batch]{run_nma_batch()}} to run the same pipeline over several outcomes;
\code{\link[=plot_transitivity]{plot_transitivity()}} for the transitivity companion plot.
}
