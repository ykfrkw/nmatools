% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netmetawrap.R
\name{netmetawrap}
\alias{netmetawrap}
\title{One-stop Network Meta-Analysis Wrapper}
\usage{
netmetawrap(
  data,
  studlab,
  treat,
  outcome,
  n,
  event = NULL,
  mean_col = NULL,
  sd_col = NULL,
  sm,
  reference.group = NULL,
  small.values = "undesirable",
  path = "./outputs",
  netmeta_args = list(),
  forest_args = list(),
  netpairwise_args = list(),
  netsplit_args = list(),
  a4_rows_per_page = 45L,
  funnel_min_studies = 10L,
  trim = TRUE,
  trim_fuzz = 30L,
  .subnet_label = NULL
)
}
\arguments{
\item{data}{Data frame in arm-level format (one row per study arm).}

\item{studlab}{Unquoted or quoted column name for study labels.}

\item{treat}{Unquoted or quoted column name for treatment.}

\item{outcome}{Outcome name used for the output sub-directory and file names.}

\item{n}{Unquoted or quoted column name for sample size.}

\item{event}{Unquoted or quoted column name for event count (binary only).}

\item{mean_col}{Unquoted or quoted column name for arm mean (continuous only).}

\item{sd_col}{Unquoted or quoted column name for arm SD (continuous only).}

\item{sm}{Effect measure: \code{"OR"}, \code{"RR"}, \code{"SMD"}, or \code{"MD"}.}

\item{reference.group}{Reference treatment (string). \code{NULL} = auto (largest n).}

\item{small.values}{\code{"undesirable"} (default) or \code{"desirable"}.}

\item{path}{Base output directory. Defaults to \code{"./outputs"}.
The function creates \verb{\{path\}/\{outcome\}/} automatically.}

\item{netmeta_args}{Named list of extra arguments forwarded to
\code{netmeta::netmeta()} or \code{netmeta::netmetabin()}, overriding defaults.}

\item{forest_args}{Named list of extra arguments forwarded to all
\code{forest()} calls, overriding defaults.}

\item{netpairwise_args}{Named list of extra arguments forwarded to
\code{netmeta::netpairwise()}.}

\item{netsplit_args}{Named list of extra arguments forwarded to
\code{netmeta::netsplit()}.}

\item{a4_rows_per_page}{Estimated max rows per A4 page for large plots.
Default: \code{45}.}

\item{trim}{Trim whitespace from output PDFs via \code{magick}. Default: \code{TRUE}.}

\item{trim_fuzz}{Fuzz parameter for \code{magick::image_trim()}. Default: \code{30L}.}

\item{.subnet_label}{Internal — subnetwork label appended to file names.}
}
\value{
Invisibly, the fitted \code{netmeta}/\code{netmetabin} object, or \code{NULL} when
subnetworks are detected.
}
\description{
Runs a complete NMA pipeline and saves all outputs. Column names can be
supplied either unquoted (like \code{meta::pairwise()}) or as strings. Non-column
arguments (\code{sm}, \code{reference.group}, etc.) are always strings.
}
\examples{
\dontrun{
# Unquoted column names (recommended)
netmetawrap(
  data            = w2i_trials,
  studlab         = id,
  treat           = t,
  outcome         = "remission_lt",
  n               = n,
  event           = r,
  sm              = "OR",
  reference.group = "Pharmacotherapy",
  small.values    = "undesirable"
)

# Quoted strings also work
netmetawrap(
  data     = w2i_trials,
  studlab  = "id",
  treat    = "t",
  outcome  = "remission_lt",
  n        = "n",
  event    = "r",
  sm       = "OR"
)
}
}
