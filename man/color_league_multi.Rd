% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_league.R
\name{color_league_multi}
\alias{color_league_multi}
\title{League tables for multiple outcomes in one xlsx file}
\usage{
color_league_multi(
  outcomes,
  cinema = NULL,
  sort_by = "alphabet",
  sort_order = NULL,
  treat_labels = NULL,
  digits = 2,
  bracket = "(",
  separator = " to ",
  wrap_ci = TRUE,
  common = FALSE,
  palette = NULL,
  palette_type = c("pastel", "classic", "colorblind", "SchneiderThoma2026", "solid"),
  trivial_range = NULL,
  fill_color = "#E2EFDA",
  header_bg = "#BFBFBF",
  file = "color_league_multi.xlsx"
)
}
\arguments{
\item{outcomes}{A named list of \code{netmeta} objects.
Names are used as sheet names (truncated to 31 characters for Excel).}

\item{cinema}{\code{NULL} (no CINeMA for any outcome), a single path/data
frame applied to all outcomes, or a named list with the same names as
\code{outcomes} (use \code{NULL} for outcomes without CINeMA).}

\item{sort_by, sort_order, treat_labels, digits, bracket, separator, wrap_ci, common}{Passed to each sheet. See \code{\link{color_league}()} for details.}

\item{palette, palette_type, header_bg}{Colour settings passed to each sheet.}

\item{trivial_range}{Numeric vector \code{c(lo, hi)} on the log scale
(for OR/RR/HR) or raw scale (for MD/SMD). Required when
\code{palette_type = "SchneiderThoma2026"}.}

\item{fill_color}{Hex colour applied to all off-diagonal cells when
\code{palette_type = "solid"} (default \code{"#E2EFDA"}, light green).}

\item{file}{Output \code{.xlsx} path (default \code{"color_league_multi.xlsx"}).}
}
\value{
Invisibly returns the \code{openxlsx} workbook object.
}
\description{
Creates one Excel workbook with one sheet per outcome.
Each sheet is a colour-coded league table identical to what
\code{\link{color_league}()} produces in single-outcome mode.
}
