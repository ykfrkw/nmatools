% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_league.R
\name{color_league}
\alias{color_league}
\title{League table coloured by CINeMA confidence ratings}
\usage{
color_league(
  x,
  cinema = NULL,
  x2 = NULL,
  cinema2 = NULL,
  x3 = NULL,
  cinema3 = NULL,
  x4 = NULL,
  cinema4 = NULL,
  label1 = NULL,
  label2 = NULL,
  label3 = NULL,
  label4 = NULL,
  sort_by = "alphabet",
  sort_order = NULL,
  treat_labels = NULL,
  digits = 2,
  bracket = "(",
  separator = " to ",
  wrap_ci = TRUE,
  common = FALSE,
  fixed = NULL,
  palette = NULL,
  palette_type = c("pastel", "classic", "colorblind", "SchneiderThoma2026", "solid"),
  trivial_range = NULL,
  fill_color = "#E2EFDA",
  fill_color2 = "#D0E4F7",
  fill_color3 = "#FCE4D6",
  fill_color4 = "#EAD1DC",
  header_bg = "#BFBFBF",
  file = "color_league.xlsx"
)
}
\arguments{
\item{x}{A \code{netmeta} object (outcome 1, lower-left triangle top row).}

\item{cinema}{Path to a CINeMA report CSV, or a data frame with columns
\code{"Comparison"} and \code{"Confidence rating"}.
Used for outcome 1.}

\item{x2}{Optional \code{netmeta} object (outcome 2, upper-right triangle
top row). Must contain the same treatments as \code{x}.}

\item{cinema2}{CINeMA data for \code{x2}.}

\item{x3}{Optional \code{netmeta} object (outcome 3, lower-left triangle
bottom row). Activates quad-outcome mode.
Must contain the same treatments as \code{x}.}

\item{cinema3}{CINeMA data for \code{x3}.}

\item{x4}{Optional \code{netmeta} object (outcome 4, upper-right triangle
bottom row). Activates quad-outcome mode.
Must contain the same treatments as \code{x}.}

\item{cinema4}{CINeMA data for \code{x4}.}

\item{label1}{Label for outcome 1; written as a note below the table.}

\item{label2}{Label for outcome 2; written as a note below the table.}

\item{label3}{Label for outcome 3; written as a note below the table.}

\item{label4}{Label for outcome 4; written as a note below the table.}

\item{sort_by}{How to sort treatments: \code{"alphabet"} (default),
\code{"pscore"}, \code{"es"}, \code{"es_rev"}, \code{"pvalue"},
\code{"zscore"}, or \code{"custom"}.}

\item{sort_order}{Custom order vector (used when \code{sort_by = "custom"}).}

\item{treat_labels}{Named character vector for renaming treatments.}

\item{digits}{Number of decimal places (default 2).}

\item{bracket}{Opening bracket character for CI (default \code{"("}).}

\item{separator}{Separator between CI bounds (default \code{" to "}).}

\item{wrap_ci}{Logical. Put CI on a new line below the estimate?
Default \code{TRUE}.}

\item{common}{Use common-effects model? Default \code{FALSE}.}

\item{fixed}{Deprecated. Use \code{common} instead.}

\item{palette}{Named list of colour pairs for CINeMA ratings.
If \code{NULL}, \code{palette_type} is used.}

\item{palette_type}{Coloring mode. One of:
\itemize{
\item \code{"pastel"} (default) — CINeMA confidence ratings, pastel colours.
\item \code{"classic"}          — CINeMA confidence ratings, vivid colours.
\item \code{"colorblind"}       — CINeMA confidence ratings, Okabe–Ito palette.
\item \code{"SchneiderThoma2026"} — CI vs trivial range (Blue/Yellow/Orange/White).
Requires \code{trivial_range}.
\item \code{"solid"}            — All off-diagonal cells filled with a single
colour. Use \code{fill_color} / \code{fill_color2} etc. to specify the colour
per outcome.
}}

\item{trivial_range}{Numeric vector \code{c(lo, hi)} on the log scale (for OR/RR/HR)
or raw scale (for MD/SMD). Required when \code{palette_type = "SchneiderThoma2026"}.}

\item{fill_color}{Hex colour for all off-diagonal cells of outcome 1 when
\code{palette_type = "solid"} (default \code{"#E2EFDA"}, light green).}

\item{fill_color2}{Hex colour for outcome 2 cells in dual/quad mode
(default \code{"#D0E4F7"}, light blue).}

\item{fill_color3}{Hex colour for outcome 3 cells in quad mode
(default \code{"#FCE4D6"}, light orange).}

\item{fill_color4}{Hex colour for outcome 4 cells in quad mode
(default \code{"#EAD1DC"}, light pink).}

\item{header_bg}{Background colour for diagonal treatment-name cells
(default \code{"#BFBFBF"}).}

\item{file}{Output \code{.xlsx} path.}
}
\value{
Invisibly returns the \code{openxlsx} workbook object.
}
\description{
Creates a colour-coded NMA league table (n x n square matrix)
where each cell is coloured according to the CINeMA confidence rating for
that comparison. The diagonal cells show treatment names on a grey
background.

\strong{Dual-outcome mode} (\code{x2} supplied): lower-left triangle
shows outcome 1 (\code{x}); upper-right triangle shows outcome 2
(\code{x2}).

\strong{Quad-outcome mode} (\code{x3} and/or \code{x4} supplied): each
off-diagonal cell is split into a top and bottom sub-row.
\itemize{
\item Lower-left top sub-row: outcome 1 (\code{x})
\item Lower-left bottom sub-row: outcome 3 (\code{x3})
\item Upper-right top sub-row: outcome 2 (\code{x2})
\item Upper-right bottom sub-row: outcome 4 (\code{x4})
}
Diagonal cells are merged vertically and show the treatment label.
}
